#include <stdio.h>
#include <stdlib.h>

int main()
{
    int a=1,b;
    printf("Input number =",b);
    scanf("%d",&b);
    if(a&b){
        printf("Least Significant Bit  is set (1).");
    }
    else{
         printf("Least Significant Bit  is set (0).");
    }


    return 0;
}
